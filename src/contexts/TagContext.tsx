// import React, { createContext, useContext } from 'react'
// // Tạo một context mới
// const TagsContext = createContext()
// import { useTags } from '../hook/useTag'

// // Tạo Provider component
// const TagsProvider = ({ meta, children }) => {
//   const { data, isLoading, error } = useTags(meta)

//   return <TagsContext.Provider value={{ data, isLoading, error }}>{children}</TagsContext.Provider>
// }

// export default TagsProvider
